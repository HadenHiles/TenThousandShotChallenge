name: ttsc_test_scripts
version: 1.0.0
publish_to: none
description: Dart CLI tools for Ten Thousand Shot Challenge test data import and automation.
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  http: any
  path: any

dev_dependencies:
  firebase_dart: ^1.4.3 # CLI-compatible Firebase (Firestore, Auth, Core)
  lints: ^3.0.0
